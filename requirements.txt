# Core
PySide6>=6.6.0
Pillow>=10.0.0

# Optional — RAW support
rawpy>=0.19.0

# Optional — FITS support
astropy>=6.0.0
# fitsio>=1.2.0  # faster C backend, uncomment if needed
numpy>=1.26.0

# Optional — memory stats
psutil>=5.9.0
